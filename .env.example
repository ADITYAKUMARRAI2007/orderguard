# Razorpay TEST mode only. Never live keys.
# Dashboard -> Account & Settings -> API Keys -> Generate Test Key
RZP_KEY_ID=
RZP_KEY_SECRET=

# Anthropic. Model id is configurable so the repo works on any account.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-opus-5
