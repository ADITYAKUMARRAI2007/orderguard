# Demo

1. Run `make app` and open `http://127.0.0.1:8000/app`.
2. Show BUY/MISSION, CONNECTORS, ATTACK LAB, EVIDENCE, FEATURES, and EVAL.
3. For commerce, select a real candidate, confirm quantity 1, and show the
   fresh authoritative cart reread before the Razorpay test order.
4. Run the quantity-mutation Attack Lab case and show the block/evidence.
5. Verify the audit chain from the Evidence screen.

Do not demo live connector/runtime claims until their status card is
connected. The current subscription token returns 401, Swiggy requires OAuth,
and GitHub requires a user-provided read-only PAT.
